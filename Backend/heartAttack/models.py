# Placeholder for future data models, if needed
# Example: To store user information, prediction results, etc.
# class Prediction(models.Model):
#     age = models.FloatField()
#     sex = models.IntegerField()
#     prediction_result = models.BooleanField()  # True = at risk, False = not at risk
#     created_at = models.DateTimeField(auto_now_add=True)
