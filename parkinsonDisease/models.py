# Placeholder for future data models, such as storing user information or prediction results
# Example:
# class Prediction(models.Model):
#     user_id = models.ForeignKey(User, on_delete=models.CASCADE)
#     prediction_result = models.CharField(max_length=255)  # e.g., 'Parkinson's' or 'No Parkinson's'
#     created_at = models.DateTimeField(auto_now_add=True)
